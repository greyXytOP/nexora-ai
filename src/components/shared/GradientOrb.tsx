// Gradient orbs are disabled in the flat 2D design component kept as no-op for import compatibility
export function GradientOrb(_props: {
  tone?: "brand" | "accent" | "fuchsia";
  size?: number;
  className?: string;
}) {
  return null;
}
